# Allowed browser origin(s) for CORS — your Vercel frontend URL.
# Comma-separate multiple origins. Defaults to "*" if unset.
# Example: https://your-wordle.vercel.app
CLIENT_ORIGIN=

# Render provides PORT automatically. Locally, uvicorn defaults to 8000.
# PORT=8000
