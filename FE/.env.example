# Base URL of the FastAPI backend (Socket.IO + definitions).
# Leave UNSET locally to default to http://localhost:8000.
# In production (Vercel), set this to your Render backend URL, e.g.
# NEXT_PUBLIC_BACKEND_URL=https://your-wordle-api.onrender.com
NEXT_PUBLIC_BACKEND_URL=
